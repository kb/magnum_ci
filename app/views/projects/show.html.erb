<%= button_to "Manual Build", { :action => "build", :id => @project }, :id => "build_button" %>
<%= link_to "Edit", "/#{@project.name}/edit" %>
OR
<%= link_to "Delete Project", project_path(@project), :confirm => "Are you sure?", :method => :delete %>
<ul>
  <li id='project_name'><h1><%= @project.name %></h1></li>
  <%= render :partial => "build", :collection => @project.builds.reverse %>
</ul>
