<% form_for @project do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <div class='buttons'>
    <%= f.submit %>
    <%= link_to 'Cancel', "/#{@project.name}" %>
  </div>
<% end %>
